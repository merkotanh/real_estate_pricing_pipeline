# Model Artifacts

This project generates trained models saved as `.pkl` files.

Artifacts include:
- trained regression model
- imputer
- feature list

Due to size and reproducibility, model files are not stored in the repository.